

Context
역할 -> ActivityManagerService에 접근 하도록 해주는 역할
-> 주변정보


안드로이드는 이미 많은 부분들(함수,라이브러리) 등이 구현이 되어있다. -> 이러한 것들을 사용하기 위해서는 Context가 필요한 경우가 많다.

종류
- activity의 context
- application의 context
- ApplicationContext > ActivityContext